# ===== Local Docker env =====
# Frontend base URL (used to set cookie secure flag)
APP_BASE_URL=http://localhost:8080
APP_DOMAIN=app.moreirasolar.com

# Session secret for cookies
SESSION_SECRET=change_me_to_a_random_secret

# OIDC (preencha se for usar SSO; para login local, mantenha variáveis válidas quaisquer)
OIDC_ISSUER_URL=http://example.local/issuer
OIDC_CLIENT_ID=example-client
OIDC_CLIENT_SECRET=example-secret
OIDC_REDIRECT_PATH=/api/auth/callback

# Use SSO (true) ou login local (false)
USE_KEYCLOAK=false

# Supabase
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY

# Misc
PORT=4000
LOGOUT_REDIRECT_PATH=/auth

# Optional: email para ACME (Caddy)
# ACME_EMAIL=dev@example.com

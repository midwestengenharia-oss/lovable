{
  email {$ACME_EMAIL}
}

{$APP_DOMAIN} {
  encode zstd gzip

  # Proxy API preserving /api prefix
  handle /api* {
    reverse_proxy api:4000
  }

  handle {
    root * /usr/share/caddy
    try_files {path} /index.html
    file_server
  }
}
